{
  "name": "webflow-snap",
  "version": "0.1.0",
  "description": "A GitHub Action to create a snapshot of Webflow sites.",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest watch",
    "coverage": "vitest run --coverage",
    "build": "ncc build src/index.js --license licenses.txt"
  },
  "author": "Viktor Tar",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.25.2",
    "vitest": "^0.25.2"
  }
}
